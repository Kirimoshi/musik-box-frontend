import styled from 'styled-components';

export const PublicPlaylistListContainer = styled.section`
  margin-top: 10px;
  overflow-y: scroll;
  overflow-x: hidden;
  display: flex;
  flex-direction: column;
  gap: 10px;
`;
